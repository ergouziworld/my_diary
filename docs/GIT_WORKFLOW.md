# Git 工作流

这个仓库推荐的本地工作流程：

1. 为变更创建一个聚焦的分支
2. 只做最小且连贯的一组修改
3. 运行相关检查
4. 提交前复查 diff
5. 如果可行，把文档更新和行为变更分开处理

工作规则：
- 未经明确要求，不要使用破坏性的 reset 命令
- 不要覆盖和当前任务无关的用户修改
- 优先做小而可审查的提交
- 如果需要清理，先确认目标路径

这个仓库同时有应用代码和文档工作。对于较大的任务，通常更安全的做法是把源码修改和文档修改放在同一分支里，但分成不同提交。
