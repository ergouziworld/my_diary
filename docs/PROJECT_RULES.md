# 项目规则

- 保持代码和文档分离。
- 不要用 Markdown 替换可执行源码。
- 文档要对齐当前实现，不只对齐长期愿景。
- 扩展文档时优先做增量修改。
- 除非明确要求重写，否则保留现有文档内容。
- 调整文档时保持文件编码为 `UTF-8 without BOM`，行尾为 `LF`。
- 除非任务明确要求清理，否则不要删除用户拥有的文件。
- 当文档和代码不一致时，以代码为实现来源，以文档为解释层。
- 记录路由、模型和文件名时必须保持精确。
